{
    "name": "restaunax-backend",
    "version": "1.0.0",
    "scripts": {
        "start": "node src/server.js",
        "migrate": "npx prisma migrate dev --name init",
        "seed": "node prisma/seed.js"
    },
    "dependencies": {
        "@prisma/client": "^5.22.0",
        "cors": "^2.8.5",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "prisma": "^5.22.0"
    }
}
